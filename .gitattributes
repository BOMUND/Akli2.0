# Принудительно используем LF для текстовых файлов проекта.
# Это нужно, чтобы diff был стабильным независимо от того, на чём редактируют исходники.
* text=auto eol=lf

# Бинарные/нетекстовые ассеты не трогаем.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
